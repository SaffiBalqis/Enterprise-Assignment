@model MMTS.Models.Session

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Session</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div class="form-group">
                <label asp-for="MentorId" class="control-label"></label>
                <select asp-for="MentorId" class="form-control" asp-items="ViewBag.MentorId"></select>
            </div>
            <div class="form-group">
                <label asp-for="MenteeId" class="control-label"></label>
                <select asp-for="MenteeId" class="form-control" asp-items="ViewBag.MenteeId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ScheduledTime" class="control-label"></label>
                <input asp-for="ScheduledTime" class="form-control" type="datetime-local" />
            </div>
            <button type="submit" class="btn btn-primary mt-3">Create</button>
            <a asp-action="Index" class="btn btn-secondary mt-3">Cancel</a>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
